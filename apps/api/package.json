{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "sh -c 'lsof -ti:8000 2>/dev/null | xargs kill -9 2>/dev/null || true; if command -v doppler >/dev/null 2>&1; then doppler run -- uv run python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000; else uv run python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000; fi'"
  }
}
